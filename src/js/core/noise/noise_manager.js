const { FastNoise } = require("./FastNoise");
const { Vector } = require("../vector");


class NoiseManager {
    /**@type object */
    noise;
    /**
     * @param  {FastNoise} object
     */
    constructor( object) {
        this.noise = object;
    }

    GetColor(){

    }

}


